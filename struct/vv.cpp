#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

int main(){
    int n;
    cin >> n;
    vector<int>vec;
    for (int i = 0; i < n; i++)
    {
        int in;
        cin >>in;
        vec.push_back(in);
    }
    int m;
    cin >> m;
    vector<int>vec1;
    for (int i = 0; i < m; i++)
    {
        int in;
        cin >>in;
        vec.push_back(in);
    }
    for (int k = 0; k < vec1.size(); k++)
    {
        for (int i = 0; i < n; i++)
        {
        for (int j = 0; j < n; j++)
        {
            if()
        }
        
    }
    }
    
    
    

    return 0;
}
