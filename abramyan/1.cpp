#include <iostream>
#include <cmath>

using namespace std;

int main() {
   string str;
   getline(cin,str);
   for (int i = 0; i < str.length(); i++)
   {
      if(str[i]>=0 and str[i]<=255 ){
         cout << "good";
      }
      else[
         cout << "bad";
      ]
   }
   

	return 0;
}
